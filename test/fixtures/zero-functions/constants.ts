// No function-like declarations at all — used to verify the CLI treats
// "zero extracted functions" as an analysis failure (exit 2), not as a
// clean check.

export const TAX_RATE = 0.1;
export const LABEL = "constants only";
