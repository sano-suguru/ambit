# No .ts files

Deliberately empty of TypeScript source — used to verify the CLI treats a
directory with zero analyzable files as an analysis failure (exit 2), not as
a clean check (DESIGN.md §3.4). This file exists only so the directory is
tracked by git.
