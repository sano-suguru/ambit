// A declaration file belonging to this project, neither shipped with the
// compiler nor installed as a package: `ambient-declaration`.
declare module "legacy-store" {
  export function readLegacy(key: string): string;
}
