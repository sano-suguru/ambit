name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          # `ambit diff` needs the base commit to exist locally, and
          # `actions/checkout` fetches a single commit by default. 2 is enough
          # for `HEAD~1`; on a pull request that is the base tip, which is the
          # comparison the step wants.
          fetch-depth: 2

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Lint & format check
        run: pnpm exec biome ci .

      - name: Type check
        run: pnpm exec tsc --noEmit

      - name: Test
        run: pnpm test

      # The gate: a contract violation in Ambit's own source fails the build.
      # `--format github` annotates the offending line with the whole call path
      # (DESIGN.md §5.1 / §6), and `--coverage` keeps recording the numbers
      # docs/status.md tracks. Not `--strict`: measured, `check src --strict`
      # exits 1 on four unresolved-call warnings in the connection layer — see
      # docs/status.md, "Ambit's own source".
      - name: Contract check on Ambit's own source
        run: node src/cli/main.ts check src --coverage --format github

      # Not a gate yet. `ambit diff` has never run in GitHub Actions, and
      # nothing here has observed one: it is added reporting-only so a real
      # run can be read first, and promoted once that run is green. The
      # annotations land on the functions whose authority grew (DESIGN.md §6).
      - name: Authority diff against the base commit (non-gating)
        continue-on-error: true
        run: node src/cli/main.ts diff HEAD~1 src --format github
